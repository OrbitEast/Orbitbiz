/* OrbitBiz — invoice detail, payments, status and PDF actions. */
(()=>{
  "use strict";
  const core=window.OrbitBiz=window.OrbitBiz||{},w=core.workspace=core.workspace||{},esc=v=>w.esc(v),money=v=>w.money(v),date=v=>v?new Date(v).toLocaleDateString("en-IN",{day:"2-digit",month:"short",year:"numeric"}):"—",today=()=>new Date().toISOString().slice(0,10);
  const statusOf=i=>{const total=Number(i.total||0),paid=Number(i.amount_paid||0),stored=i.status||"draft";if(stored==="cancelled")return"cancelled";if(total>0&&paid>=total)return"paid";if(paid>0)return"partial";if(i.due_date&&i.due_date<today()&&stored!=="draft")return"overdue";return stored};
  w.invoiceDetailData=async(state,id)=>{const db=w.db();if(!db||!state.businessId||!id)throw Error("Invoice is not available");const ir=await db.from("invoices").select("*").eq("id",id).eq("business_id",state.businessId).maybeSingle();if(ir.error)throw ir.error;if(!ir.data)throw Error("Invoice could not be found");const invoice=ir.data,[cr,lr,pr]=await Promise.all([invoice.customer_id?db.from("customers").select("id,name,company_name,email,phone,gstin,billing_address,shipping_address").eq("id",invoice.customer_id).eq("business_id",state.businessId).maybeSingle():Promise.resolve({data:null,error:null}),db.from("invoice_items").select("id,item_id,description,quantity,unit_price,discount,tax_rate,line_total,created_at").eq("invoice_id",id).order("created_at"),db.from("payments").select("id,payment_date,amount,method,reference,notes,created_at").eq("business_id",state.businessId).eq("invoice_id",id).order("payment_date",{ascending:false}).limit(100)]);if(cr.error)throw cr.error;if(lr.error)throw lr.error;if(pr.error)throw pr.error;return{invoice,customer:cr.data,lines:lr.data||[],payments:pr.data||[],status:statusOf(invoice)}};
  w.invoiceDetailView=data=>{const{invoice,customer,lines,payments,status}=data,total=Number(invoice.total||0),paid=Number(invoice.amount_paid||0),balance=Math.max(0,total-paid),tone=status==="paid"?"done":status==="partial"?"blue":status==="overdue"?"pink":status==="cancelled"?"open":"yellow";return`<section class="ob-invoice-detail-head"><div><button class="ob-soft" data-action="back-invoices">${w.icon("arrow")} Invoices</button><div class="ob-invoice-title"><span class="ob-eyebrow">SALES DOCUMENT</span><h2>${esc(invoice.invoice_number)}</h2><span class="ob-status ${tone==="done"?"done":"open"}">${esc(status.replace(/_/g," "))}</span></div><p>${esc(customer?.company_name||customer?.name||"Customer")}</p></div><div class="ob-profile-actions"><button class="ob-soft" data-action="print-invoice">Print</button><button class="ob-soft" data-action="invoice-pdf">PDF</button>${status!=="cancelled"&&balance>0?`<button class="ob-primary" data-action="record-payment">${w.icon("plus")} Record payment</button>`:""}</div></section>${status!=="cancelled"?`<div class="ob-invoice-lifecycle"><span>STATUS</span>${["draft","sent","overdue"].map(s=>`<button class="ob-lifecycle-step ${status===s?"active":""}" data-action="invoice-status" data-status="${s}" ${status==="paid"||status==="partial"?"disabled":""}>${s}</button>`).join("")}<span class="ob-lifecycle-arrow">→</span><button class="ob-lifecycle-step" data-action="invoice-status" data-status="cancelled">Cancel</button></div>`:""}<section class="ob-invoice-detail-grid"><article class="ob-panel ob-invoice-paper" id="invoice-print-area"><header class="ob-paper-top"><div><span>ORBITBIZ</span><h3>INVOICE</h3></div><div><strong>${esc(invoice.invoice_number)}</strong><small>Issued ${date(invoice.issue_date)}</small>${invoice.due_date?`<small>Due ${date(invoice.due_date)}</small>`:""}</div></header><div class="ob-paper-parties"><div><small>BILL TO</small><strong>${esc(customer?.name||"Customer")}</strong>${customer?.company_name?`<span>${esc(customer.company_name)}</span>`:""}${customer?.phone?`<span>${esc(customer.phone)}</span>`:""}${customer?.email?`<span>${esc(customer.email)}</span>`:""}${customer?.gstin?`<span>GSTIN: ${esc(customer.gstin)}</span>`:""}</div><div><small>STATUS</small><strong>${esc(status.replace(/_/g," "))}</strong><span>Created ${date(invoice.created_at)}</span></div></div><div class="ob-paper-table"><table class="ob-table"><thead><tr><th>Description</th><th>Qty</th><th>Rate</th><th>Discount</th><th>Tax</th><th>Total</th></tr></thead><tbody>${lines.map(x=>`<tr><td><strong>${esc(x.description)}</strong></td><td>${Number(x.quantity||0).toLocaleString("en-IN")}</td><td>${money(x.unit_price)}</td><td>${money(x.discount)}</td><td>${Number(x.tax_rate||0)}%</td><td><strong>${money(x.line_total)}</strong></td></tr>`).join("")}</tbody></table></div><div class="ob-paper-bottom"><div>${invoice.notes?`<small>NOTES</small><p>${esc(invoice.notes)}</p>`:""}</div><div class="ob-paper-totals"><div><span>Subtotal</span><b>${money(invoice.subtotal)}</b></div><div><span>Discount</span><b>${money(invoice.discount_total)}</b></div><div><span>Tax</span><b>${money(invoice.tax_total)}</b></div><div class="grand"><span>Total</span><strong>${money(total)}</strong></div></div></div></article><aside class="ob-invoice-side"><article class="ob-panel"><div class="ob-panel-head"><div><span>ACCOUNT</span><h3>Payment position</h3></div></div><div class="ob-payment-position"><div><small>TOTAL</small><strong>${money(total)}</strong></div><div><small>RECEIVED</small><strong>${money(paid)}</strong></div><div class="balance"><small>OUTSTANDING</small><strong>${money(balance)}</strong></div></div></article><article class="ob-panel"><div class="ob-panel-head"><div><span>PAYMENTS</span><h3>${payments.length?`${payments.length} recorded`:"No payments"}</h3></div></div>${payments.length?`<div class="ob-list">${payments.map(x=>`<div class="ob-list-row"><div class="ob-list-avatar tone-2">${w.icon("finance")}</div><div class="ob-list-main"><strong>${money(x.amount)}</strong><small>${date(x.payment_date)} · ${esc(String(x.method||"bank_transfer").replace(/_/g," "))}${x.reference?` · ${esc(x.reference)}`:""}</small></div><span class="ob-status done">Received</span></div>`).join("")}</div>`:`<div class="ob-empty compact"><div>${w.icon("finance")}</div><h4>No payment yet</h4><p>Record a payment when money is received.</p></div>`}</article></aside></section>`};
  w.recordInvoicePayment=(state,data)=>{const invoice=data.invoice,balance=Math.max(0,Number(invoice.total||0)-Number(invoice.amount_paid||0)),node=w.modal("Record payment",`<form class="ob-form ob-payment-form"><div class="ob-payment-callout"><span>OUTSTANDING</span><strong>${money(balance)}</strong><small>For ${esc(invoice.invoice_number)}</small></div><div class="ob-form-grid"><label class="ob-field"><span>Amount</span><input name="amount" type="number" min="0.01" max="${balance.toFixed(2)}" step="0.01" value="${balance.toFixed(2)}" required></label>${w.field("Payment date","payment_date","date",true,today())}<label class="ob-field"><span>Method</span><select name="method"><option value="bank_transfer">Bank transfer</option><option value="upi">UPI</option><option value="cash">Cash</option><option value="card">Card</option><option value="cheque">Cheque</option><option value="other">Other</option></select></label><label class="ob-field"><span>Reference</span><input name="reference" placeholder="Transaction / receipt reference"></label></div>${w.textarea("Notes","notes","","Optional payment note")}<button class="ob-primary" type="submit">Save payment ${w.icon("arrow")}</button></form>`);node.querySelector("form").addEventListener("submit",async e=>{e.preventDefault();const raw=Object.fromEntries(new FormData(e.currentTarget).entries()),amount=Number(raw.amount||0);if(!(amount>0)||amount>balance){w.toast("Enter a valid amount within the outstanding balance");return}const btn=e.currentTarget.querySelector("button[type=submit]");btn.disabled=true;btn.textContent="Saving…";try{const r=await w.db().rpc("record_invoice_payment",{p_business_id:state.businessId,p_invoice_id:invoice.id,p_payment_date:raw.payment_date,p_amount:amount,p_method:raw.method,p_reference:raw.reference||null,p_notes:raw.notes||null});if(r.error)throw r.error;node.remove();w.toast("Payment recorded");await state.renderPage()}catch(err){w.toast(err.message||"Could not record payment");btn.disabled=false;btn.innerHTML=`Save payment ${w.icon("arrow")}`}})};
  w.updateInvoiceStatus=async(state,id,status)=>{const r=await w.db().rpc("update_invoice_status",{p_invoice_id:id,p_status:status});if(r.error)throw r.error;w.toast(`Invoice marked ${status}`);await state.renderPage()};
  document.addEventListener("click",async e=>{const b=e.target.closest("[data-action]");if(!b)return;const s=core.workspaceState;if(!s)return;try{const id=s.invoiceId;if(b.dataset.action==="invoice-pdf"){if(!id)throw Error("Invoice could not be identified");await w.prepareInvoicePdf(s,await w.invoiceDetailData(s,id))}else if(b.dataset.action==="print-invoice")window.print();else if(b.dataset.action==="record-payment"){if(id)w.recordInvoicePayment(s,await w.invoiceDetailData(s,id))}else if(b.dataset.action==="invoice-status"&&id)await w.updateInvoiceStatus(s,id,b.dataset.status)}catch(err){console.error(err);w.toast(err.message||"Could not complete invoice action")}});
})();
